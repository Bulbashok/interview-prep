# Дата: 2026-03-22

Сегодня добавил обработку ошибок во все методы firestoreService.Обернул в try/catch и создал функцию handleFirestoreError для понятных сообщений. Теперь при проблемах с Firestore пользователь увидит осмысленный текст вместо белого экрана.

Исправил баг с бесконечной загрузкой профиля. Проблема была в useFirebaseAuth: если fetchUserData выбрасывал ошибку, setLoading(false) никогда не вызывался. Обернул в try/catch/finally, теперь loading всегда сбрасывается.

Сделал страницу-заглушку для профиля с LinearProgress индикатором и кнопкой «На главную». ProtectedRoute теперь принимает кастомный loadingComponent что позволяет показывать разные loading state для разных страниц.

Добавил экран перенаправления для login/register теперь когда авторизованный пользователь пытается зайти на эти страницы, видит «Перенаправление...» и кнопку «на главную». Навигация через useEffect, чтобы редирект отрабатывал корректно.

Добавил все недостающие i18n ключи для dashboard и profile страниц в ru/en локалях.
